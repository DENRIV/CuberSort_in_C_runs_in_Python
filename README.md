# CuberSort_in_C_runs_in_Python

# Cubesort

### C Version  
# gcc -v

### compile program
# gcc cubesort.cpp

### It's created a file:
# cubesort.exe

### Commands (only apply if it's needed to work withctypes)
# gcc -c -fPIC cubesort.cpp -o cubesort.o
# gcc -shared -Wl,-soname,libcubesort.so -o libcubesort.so  cubesort.o
